<div class="flex text-rose text-xl gap-1.5">
    <p><span class="font-bold">{{ question.nbAnswer }}</span> réponse{% if question.nbAnswer > 1 %}s{% endif %}</p>
    <span> - </span>
    <p>Par {{ question.author.username }}</p>
</div>