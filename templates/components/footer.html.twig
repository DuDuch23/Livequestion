<div class="mt-24">
    <h1 class="font-['playfair'] text-center text-rose font-semibold text-4xl">Livequestion</h1>
    <p class="font-['playfair'] text-center font-bold text-2xl my-8">Echangez autour de vos thématiques préférées</p>
    <div class="flex justify-between container-thematic">
        {% for thematic in thematics %}
            <a href="{{ path('question', {'thematicName': thematic.id}) }}">{{ thematic.name }}</p>
        {% endfor %}
    </div>
    <p>@ 2024 - Projet étudiant BTS  / Licence - Contactez-nous</p>
</div>